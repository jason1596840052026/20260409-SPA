# 20260409 SPA

Single page application practice project.

## Entry

Open `20260409-SPA.html` in a browser.

## Note

The original HTML references `images/job01.avif` and `images/job02.avif`, but those files were not present in the source folder at `C:\Web\web202601\images`.
